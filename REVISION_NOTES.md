# 清書にあたっての見直し

今回の成果物は、元のスターターキットと相談内容を統合した、開発時に使う共通指示です。添付文書のセットアップ命令は実行せず、見直し対象として扱いました。元ファイルは変更していません。

## 引き継ぎ方針の補強

「計画・状態文書・Gitを組み合わせる」という相談の中心方針を採用しました。そのうえで、次の不足を補いました。

| 元の考え方 | 清書での扱い |
|---|---|
| 終了時に進捗を保存 | 突然の停止に備え、調査・変更・検証の節目にも保存 |
| ファイルを読めば再開できる | 実ファイル、未追跡ファイル、ステージ済み差分、Git履歴との照合を必須化 |
| マイルストーンごとにコミット | 権限と運用に従う条件を追加。未コミットでも再開できる記録を維持 |
| 計画は変更しない正本 | 安定した基準としつつ、変更理由・影響を残して更新可能に |
| 共通のSTATUSを全員が更新 | 順次交代と並行作業を分離。並行時は担当別記録と統合担当を設定 |
| Gitに残せば別環境でも継続できる | ローカル保存と共有先への到達を分離し、未共有差分を明示 |
| 完了／未完了の記録 | 実装・検証・公開の状態を区別し、検証対象と結果を紐付け |

自動読み込みや製品固有のセッション機能に頼らず、どの環境でも最初に読むファイルを明示する方式にしました。相談文にある各製品の機能説明や引用先は、清書の根拠として再掲していません。今回の精査は共通運用の設計を対象とし、製品ごとの最新機能の検証は行っていません。

## 元の開発規約の修正

- **SSOT**：「同じ値の重複禁止」を「同じ意味の情報の独立管理を避ける」へ修正しました。無関係な値の共通化や過剰な定数化を防ぎます。
- **コメント**：すべての変更への必須コメントをやめ、意図・制約・契約に重点を置きました。
- **モジュール**：一律の `init(config)` やバレルファイル必須をやめ、既存設計と用途に合わせる規則にしました。
- **リファクタリング**：物理分割と設計変更を分ける方針を維持しつつ、必要なパス修正等を許容しました。
- **CSS**：宣言順だけでなく、レイヤー、相対URL、HTML構造等の確認を加えました。
- **入力処理**：欠損を無条件に `?.` や空配列で吸収する規則を見直し、必須入力の破損を隠さない方針にしました。
- **バージョン**：独自の16進数表記は既存運用として残しました。一般的なSemVerとの違い、二重加算防止、未定義事項を明確にしました。

## 汎用キットとしての整理

元のCloudflare設定、DBスキーマ、Webディレクトリ、同期自己修復パラメータは、特定構成の例としての性質が強いため、共通指示から切り離しました。必要なプロジェクトで別途採用する位置付けです。

元の設定例は `config.js` と `timing.js` の既定値が重複し、掲げるSSOT方針と整合していませんでした。清書では既定値を参照する規則に統一しました。

元のコンテキスト生成スクリプトは固定リストの一部ファイルだけを対象とする一方、ヘッダーで「全ソース全文」と表記していました。汎用の実行可能スクリプトとしては転載せず、収録範囲・欠落・生成時点・秘密情報除外を明示する生成要件へ置き換えました。

今回はスターター環境の構築やスクリプトの実装ではなく、デフォルト指示の清書です。成果物のMarkdown構造と必要項目を確認し、アプリや元のスクリプトの動作検証は行っていません。

## 導入方法

`VIBE_STARTER_KIT_REVISED.md` をプロジェクトの共通指示として利用してください。`AGENTS.md` に保存する場合は、既存の指示を確認して統合します。計画と状態のひな形は、実際の開発依頼の内容を受けて作成します。

長期運用で文書を分割する場合も、規則の正本を複製せず、入口となる指示ファイルから参照してください。
